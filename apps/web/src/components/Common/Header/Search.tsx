import React from "react";
import { Button } from "@components/UI/Button";
import { Input } from "@components/UI/Input";
import type { FC } from "react";

const Search: FC = () => {
  return (
    <div>
      {/* <Button variant="warning">dsa</Button> */}
      <Input></Input>
    </div>
  );
};

export default Search;
